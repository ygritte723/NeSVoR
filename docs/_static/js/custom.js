/*
window.onload = function () {
    var kbds = document.getElementsByTagName("kbd");
    for (let kbd of kbds) {
        let txt = kbd.textContent;
        txt = txt.replace(/[\W_]+/g, "-").split("-").filter(element => element).join("-");
        kbd.id = txt;
    }
}
*/
